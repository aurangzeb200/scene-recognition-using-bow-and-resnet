# scene-recognition-using-bow-and-resnet
Scene Recognition Pipeline comparing Bag of Visual Words (SIFT + K-Means) and Deep Learning (ResNet-18). Implements multi-class classification using KNN and One-vs-All SVMs with t-SNE feature visualization.
